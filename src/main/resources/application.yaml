spring:
  application:
    name: IATrainig
  aop:
    auto: true
    proxy-target-class: true
  data:
    mongodb:
      uri: "${MONGODB_URI}"
  jackson:
    property-naming-strategy: SNAKE_CASE
    default-property-inclusion: non_null
